@page
@model TheEleganceShop.Pages.OrderDetails.IndexModel

@{
    ViewData["Title"] = "Your Ordered Items";
}

<style>
    body, th, td {
        background-color: black;
        font-weight: 400;
        text-align: center;
        color: white;
    }
</style>


@if (User.IsInRole("Admin")){
    <h1> All Ordered Products & Details </h1>
}

else{
<h1> Your Ordered Items  </h1>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderDetail[0].ProductQuantity)
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.OrderDetail[0].OrderHeader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderDetail[0].Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderDetail) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductQuantity)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.OrderHeader.OrderHeaderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.ProductName)
            </td>
            <td>
                
                <a asp-page="./Details" asp-route-id="@item.OrderDetailID">Details</a> 
                
            </td>
        </tr>
}
    </tbody>
</table>

@if(Model.OrderDetail.Any()){
<h3> The chill guy thanks you for your purchase! </h3>
<iframe src="https://www.tiktok.com/embed/7440907340171185430" frameborder="0" width="325" height="580"></iframe>

            <p> I do not own any rights to this meme.</p>
}